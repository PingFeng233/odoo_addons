<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="custom_payment.list">
      <field name="name">custom_payment list</field>
      <field name="model">custom_payment.custom_payment</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="custom_payment.action_window">
      <field name="name">custom_payment window</field>
      <field name="res_model">custom_payment.custom_payment</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="custom_payment.action_server">
      <field name="name">custom_payment server</field>
      <field name="model_id" ref="model_custom_payment_custom_payment"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="custom_payment" id="custom_payment.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="custom_payment.menu_1" parent="custom_payment.menu_root"/>
    <menuitem name="Menu 2" id="custom_payment.menu_2" parent="custom_payment.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="custom_payment.menu_1_list" parent="custom_payment.menu_1"
              action="custom_payment.action_window"/>
    <menuitem name="Server to list" id="custom_payment" parent="custom_payment.menu_2"
              action="custom_payment.action_server"/>
    -->
  </data>
</odoo>